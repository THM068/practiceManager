package com.practiceManager.pages
import geb.Page

class LoginPage extends Page{

    static at = { title == "Login" }
    static url ="practiceManager/login/auth"

    static content = {
        username { $('#username') }
        password { $('#password') }
        submitButton { $('input',type:'submit')}
    }


}
